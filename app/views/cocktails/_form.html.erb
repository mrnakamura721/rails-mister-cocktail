
<%= simple_form_for(cocktail) do |f| %>
  <div class="form-inputs">
    <%= f.input :name %>
  </div>
  <%= f.input :photo, as: :file %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
